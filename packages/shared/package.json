{
  "name": "@api-demo/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for api-demo monorepo",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.1.0"
  },
  "files": [
    "dist"
  ]
}
